# 2026-09-02 遊戲體驗改造紀錄

本輪功能均已由使用者在 DOSBox-X 實機確認正常。

## 可互動熱區

- 在 3D 探索與城鎮／室內場景按住 `Tab`，標出目前有效的可互動物件；放開即清除。
- 每次按下都由現行畫面重新建立命中區，不沿用上一個場景或已消失物件的資料。
- 框線限制在 3D 世界視窗內，不會畫到羅盤、角色頭像或功能按鈕。
- 移除不必要的滑鼠左右鍵操作文字提示。
- 詳細實作見 [`contextual-hotspot-hints.md`](contextual-hotspot-hints.md)。

## 法術瀏覽器

- 六個學派圖示右下角顯示該類型已學會的法術數量。
- 選到尚未學會任何法術的分類時，清單區顯示「尚未學習」。
- 修正小字型缺少法術名稱字形，以及「尚未學習」初版編碼錯誤。
- 引擎提交：`d0ed9ec`、`83e3140`。

## 輪替自動存檔

- 五種安全觸發點共用自動存檔排程。
- 自動建立並使用 `GAMES\AutoSave.G99`，固定輪替 `SAVE01`～`SAVE10`，不碰手動存檔資料夾。
- 顯示名稱採 `YYYYMMDD_N`；載入清單依標題由新到舊排列。
- 寫入使用暫存檔與備份替換，降低中途中斷造成壞檔的風險。
- 詳細設計與驗證見 [`autosave.md`](autosave.md)。引擎提交：`96df398`、`cc5b09b`、`f3da652`、`8585fb3`。

## 已發現地下入口的地圖記號

- 玩家在 3D 畫面看見登錄的物件後，寫入持久事件旗標；之後在區域地圖顯示範圍菱形。
- 最初只登錄 **zone 1、shape 50** 的洞穴入口；第一版曾將小菱形校準在入口立面，後來改為以物件原點為中心的大型範圍標記。
- 旗標為 `LANDMARK_DISCOVERED(1..11)`，基底 macro 由 6332 開始；既有七處固定沿用 1～7，新增項目使用 8～11，舊存檔仍相容。
- 此機制是明確的地標目錄，不會自動把所有模型當成洞穴。擴充其他入口時需新增 zone／model／object／event 記錄。
- 主要引擎提交：`1bed08a`（功能建立）至 `397902a`（最終位置校準）。中間的 diagnostic commits 已保留於引擎歷史，最終 tree 不含診斷輸出。

### 十一個洞穴／礦坑／地城地表據點

完整交叉掃描十章的戶外 hotspot、`DEF_ZONE.DAT`、`TELEPORT.DAT`、`DIAL_Z23`、GDS 城鎮場景與 WLD 擺放後，確認不能只靠模型名稱判定。五處是戶外直接切換至 zone 10／12 的地下入口；兩處是具有固定互動對話的 `entrance`（即使永久封閉或尚未解鎖仍依需求保留）；另有四個地表據點要先進入 2D 場景，才會抵達下水道、礦坑、地牢或瀑布洞穴。

薩斯與克朗多各有兩個室內地下入口，但它們在戶外俯瞰地圖上共用同一個地表據點，因此各合併成一個範圍標記。純劇情俘虜／傳送事件沒有可供玩家尋找與重複使用的實體入口，已排除。

| zone | shape／模型 | 事件 | 物件／標記中心 | 資料判定 |
|---:|---|---:|---:|---|
| 1 | 50 `entrance` | 1 | `(646400, 864000)` | 直接進入地下區域 |
| 2 | 50 `entrance` | 2 | `(1132800, 1145600)` | 固定互動；封閉入口 |
| 5 | 51 `stonemtn` | 4 | `(1440000, 1081600)` | 直接進入地下區域 |
| 6 | 50 `entrance` | 3 | `(761600, 723200)` | 固定互動入口 |
| 8 | 51 `stonemtn` | 5 | `(672000, 876800)` | 直接進入地下區域 |
| 8 | 51 `stonemtn` | 6 | `(1017600, 659200)` | 直接進入地下區域 |
| 9 | 50 `stonemtn` | 7 | `(838400, 812800)` | 直接進入地下區域 |
| 2 | 87 `t020703` | 8 | `(1120000, 864000)` | 薩斯地表據點；洞穴／地下礦坑 |
| 2 | 92 `t020900` | 9 | `(1248000, 672000)` | 克朗多地表據點；兩個下水道入口 |
| 4 | 39 `fall1` | 10 | `(672000, 889600)` | 瀑布後方洞穴 |
| 6 | 87 `t060407` | 11 | `(928000, 1120000)` | 薩薩戈斯地表據點；地牢 |

- `d702c75` 將目錄擴充為三筆；`90091ea` 改以物件原點為中心，並用 `Shape.radius << Shape.shift` 的模型世界半徑換算畫面菱形半徑。畫面半徑限制為 7～36 像素，避免遠景不可見或近景遮滿畫面。
- `949ca96` 修正實機驗收發現的兩個消失問題：移除與發現狀態無關的 300000 世界座標距離裁切；投影改用原版偵測法術紅點的純俯瞰公式，不再受 3D actor pitch／depth 影響。大型菱形只要與地圖視窗相交就會繪製，超出部分交由 `draw_line()` 裁切，不再要求整顆菱形都位於視窗內。
- 使用者已實機確認上述三處版本的離遠留存與各縮放層級顯示正常。`ec9c3b4` 加入四個 `stonemtn`；`2648f99` 再依完整的切換／場景資料稽核加入四個非山體地表據點，且保留原事件編號以維持既有存檔相容性。
- Borland 3.1 重編完成：`KRONDOR.EXE` 467280 bytes，SHA-256 `de9670797f12432b2abca4f7c98cd011243e5eb371039e4da746bee5fd16452c`；`VMCODE.OVL`／`SX.OVL` byte-identical。
- Python 測試：100 passed；測試 EXE 已部署。差分補丁與整合包等實機驗收後再重建。
- **待驗收**：確認 zone 5、zone 8（兩處）、zone 9 與新增的薩斯、克朗多、瀑布、薩薩戈斯據點，在看見後會出現範圍菱形並持久留存。驗收前不更新 `engine-patches/` 的封存基準。

## 法術察覺興趣點留存

- 支援三種原版世界察覺法術：`伊夏之眼`（容器）、`The Unseen`（食物）與 `Nacre Cicatrix`（魔法物品）。只有法術原本確實畫出紅點的世界物件才會被記錄，因此座標直接沿用遊戲物件本身，不需要另建人工座標表。
- 關閉法術介面後，已察覺物件會在普通俯瞰地圖以原版風格的小紅點顯示；投影沿用俯瞰地圖座標與視野比例，因此能隨縮放與平移正確移動。
- 首版只接受 `g_pVisible_entry_pool` 與 `g_pFixed_object_entries` 的穩定物件；實機因而出現同一次法術兩個紅點只留下一筆的情形。當時的 `TEMP.GAM` 可直接驗證只寫入 zone 1／shape 162／`(672800,876000)`。修正版依「法術實際顯示即留存」規則，也記錄 `g_apCombat_zone_actor_lists` 遭遇清單的紅點；這類目標若之後移動，地圖保留的是施法當下的位置。
- 記錄以 zone、shape、世界 x/y 去重，並合併三種察覺類別遮罩。資料附加在 `TEMP.GAM` 原始內容之後，存檔與讀檔改為接受基準大小到擴充上限之間的可變長度；舊存檔沒有擴充標頭時直接視為空清單，無須轉檔。
- 最多保存 1600 筆。離線掃描十章 WLD 的法術候選固定物件為 1050 筆；遭遇清單目標通常短暫且以 zone／shape／座標去重，實機驗收時仍需留意長期存檔是否接近上限。
- 引擎提交：`e9887bf85a84a32a7a7a95e7a069f18bd01d5a2f`；舊檔檔尾讀取邊界修正：`b15752206457a36ba01809299a1da8f1303a6057`。
- 遭遇清單漏接修正：`0076d4dd3d73f0f46d48411ca129f12a1825f974`。Borland 3.1 重編完成：`KRONDOR.EXE` 468624 bytes，SHA-256 `7ffba1b175a05942d5d5963f322c01aceef68b9c5803ed55726c45e9fdbba8d4`；`VMCODE.OVL`／`SX.OVL` byte-identical。Python 測試 100 passed。
- **待驗收**：施放任一察覺法術，看見紅點後關閉法術畫面，再開普通俯瞰地圖確認紅點仍在；接著存檔、離開遊戲並重新讀檔，確認同一紅點仍保留。

## 俯瞰地圖現代鍵盤操作

- `W/S`：前進／後退。
- `A/D`：左轉／右轉。
- `Q/E`：向左／向右平移。
- `T`：紮營。
- 原有方向鍵、滑鼠操作維持不變。
- `REQ_MAP.DAT` 的紮營圖示 action 由 `0x12`（E）改為 `0x14`（T），並由 `localization/translated/MENUPAGE.json` 的 action override 重建。
- 引擎提交：`6fcffc0`；主專案資源提交：`9b0a5f1`。

## 最終建置狀態

- Python 測試：100 passed。
- `dist/test_v100_zh/krondor.exe`：468624 bytes。
- SHA-256：`7ffba1b175a05942d5d5963f322c01aceef68b9c5803ed55726c45e9fdbba8d4`。
- `dist/test_v100_zh/REQ_MAP.DAT` entry 8 action：`0x14`。
- 引擎最終提交：`0076d4dd3d73f0f46d48411ca129f12a1825f974`。
- 引擎最終 tree：`25397f7ee145e7af641ca665e03e338abddb8e33`。
- Windows 與 WSL 引擎工作樹原有的 VESA／EVG 實驗修改均保留為未提交 WIP，未混入本輪提交或封存補丁。

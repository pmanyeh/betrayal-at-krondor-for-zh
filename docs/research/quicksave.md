# 快速存檔／讀檔

## 玩家操作

- 正常 3D 探索時按 `F5`：建立一份快速存檔。
- 正常 3D 探索時按 `F9`：直接讀取最新且有效的快速存檔。
- 對話、戰鬥、城鎮／室內場景、選單及過場期間不攔截這兩個按鍵。

## 儲存配置

快速存檔與既有自動存檔完全分離：

```text
GAMES\QSave.G98\SAVE01.GAM
...
GAMES\QSave.G98\SAVE20.GAM
```

每筆標題沿用自動存檔已驗證的 `YYYYMMDD_sequence` 格式。未滿 20 槽時優先使用第一個空槽；全部已滿時，以標題日期及序號判定並覆寫最舊的一筆。檔名編號只代表實體槽位，不代表時間順序。

`QSave.G98` 可由一般「讀取進度」畫面選取，方便玩家手動回到較早的快速存檔；它刻意不出現在「儲存進度」畫面，也不能接收書籤，以免一般手動檔案破壞輪替資料的時間標題。

## 寫入安全

快速存檔沿用 `mainmenu_save_party_to_tmp()` 取得完整世界／角色／物品狀態，再採以下提交程序：

1. 寫入 `QSNEW.GAM`。
2. 驗證標頭與檔案大小符合正式存檔格式。
3. 將目標槽暫時更名為 `QSBnn.GAM`。
4. 將新檔更名為正式的 `SAVEnn.GAM`。
5. 成功後刪除備份；若更名失敗，恢復原目標檔。

每次使用前也會清理未完成的暫存檔，並恢復只剩備份、沒有正式檔案的中斷狀態。

## 載入安全

`F9` 會逐一驗證 20 個槽位，忽略截斷、版本不符或標題格式錯誤的檔案，選出日期／序號最新者。選定後只設定現有存檔槽全域值，並令 `world3d_main_loop()` 回傳既有的 mode 3；上層 `gmain_start_dispatch()` 會先走完整的場景卸載流程，之後才呼叫原本的 `savegame_read()`。這避免在活躍世界資源仍存在時直接替換遊戲狀態。

目前依需求採直接載入，不另外顯示確認視窗。`F5` 寫入成功後會在 3D 視窗底部短暫顯示「快速存檔完成」；`F9` 找到有效檔案後會先顯示「正在讀取快速存檔……」，再進入既有載入流程。兩者約兩秒後自動繼續，不需要玩家按鍵。找不到任何有效快速存檔時，仍沿用既有「此目錄沒有可讀取存檔」訊息，遊戲繼續留在目前場景。

## 實作位置

- `SRC/SCREENS/MAINMENU.C`：20 槽掃描、寫入、復原、提交及最新檔選擇。
- `SRC/SCREENS/MAINMENU.H`：快速存讀檔入口。
- `SRC/GAME/WORLD/WORLDLP.C`：`F5`／`F9` 掃描碼與安全的世界迴圈退出。

## 驗證狀態

- Borland C++ 3.1 編譯／Turbo Link 5.1 連結完成，無新增編譯錯誤。
- 使用者已確認 F5 快存、F9 快讀及兩秒提示的畫面效果正確。
- `KRONDOR.EXE`：472576 bytes，SHA-256 `94bede9adf7d803578edafb2cb6e59ed4b31e0f7866f65a8cc5ea71b59e1f65f`。
- `ZHSTAT.DAT`：991 glyph／21812 bytes，SHA-256 `ccd2a69a5c3344c043758581a7ae8af0a37238bf1934dc954b1c99c686763fb8`。
- `VMCODE.OVL`、`SX.OVL` 與基準 byte-identical。
- Python 測試：100 passed。
- 正式 bspatch：56326 bytes，SHA-256 `3fb059fe40d41acaeae4e10f10dd5c1fa448880fa5e9089abdf63b509ef98877`；由乾淨 v1.00 EXE 往返套用後與目標 EXE byte-identical。
- 正式發布 ZIP：33831092 bytes，SHA-256 `39e114bd29d907ee1ab0523b1fed251a19e8bf4df3cbee84fe35f1b3ce833fa8`；ZIP 內 `.GAM` 檔案數為 0。

### 實機驗收清單

- [x] 按 `F5` 後建立 `QSave.G98` 與第一份快存。
- [x] 移動或改變物品後按 `F9`，回到最近一次 `F5` 的狀態。
- [x] `F5` 成功後可看見「快速存檔完成」，且約兩秒自動消失。
- [x] `F9` 成功後可看見「正在讀取快速存檔……」，之後正常進入快存。
- [ ] 普通讀檔畫面可看見 QSave 及其中的快存。
- [ ] 普通存檔畫面不列出 QSave，書籤也不寫入 QSave。
- [ ] 建立超過 20 次後仍只有 20 份正式快存，且最舊一份被替換。
- [ ] 最新檔損毀或截斷時，`F9` 能退回下一份有效快存。

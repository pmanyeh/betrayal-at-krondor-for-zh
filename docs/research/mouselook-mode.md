# 可切換滑鼠視角模式

## 目標

在不改變預設操作的前提下，替一般 3D 探索加入可選用的滑鼠視角操作。這仍是原引擎的格狀移動與事件系統，不改成連續物理移動；所有人物、門、容器、屍體與劇情物件仍走既有滑鼠左鍵互動派送。

## 測試版操作

- `F2`：切換滑鼠視角模式；每次啟動遊戲時預設關閉，本次遊戲期間保留狀態。
- 滑鼠左右移動：水平旋轉視角；第一版不加入抬頭／低頭。
- `W`／`S`：前進／後退。
- `A`／`D`：向左／向右平移；方向鍵仍保留左轉／右轉。
- `Q`：保留向左平移；`E` 改為啟用準星指向的物件。
- 滑鼠左鍵：在捕捉狀態下同樣啟用準星指向的物件。
- 按住 `Ctrl`：暫停視角控制、隱藏準星並恢復原先 UI 游標位置；放開後重新捕捉。
- 按住 `Tab`：繼續顯示全部有效熱區；準星目前指向的物件以黃色框線標示。

準星使用 3D viewport 的中心，不使用整個 320×200 畫面的中心。無目標時是灰色，有目標時轉為黃色。模式切換會顯示兩秒中文提示。

## 實作原則

- `WCURSOR.C` 將原本的滑鼠互動拆成座標目標解析與互動執行。一般模式傳入實際游標位置；滑鼠視角模式傳入 viewport 中心，因此兩種操作共用完全相同的物件優先順序與事件函式。
- 中心感應只接受原本滑鼠即可互動的 `WorldHotspot` 類型；多個框重疊時沿用原本由後往前掃描的命中優先序。
- 視角捕捉只存在於 `world3d_main_loop()`。開啟施法、地圖、世界地圖、紮營、存檔選單、角色資訊、物品欄或物件互動前會先恢復游標，返回 3D 後再捕捉。
- `A`／`D`／`E` 只在實際鍵盤鍵按下時重映射；用 `Ctrl` 恢復游標後點擊畫面上的原生方向按鈕，按鈕 action 不會被誤改。
- `F2` 與 `E` 都有按鍵邊緣防重複，按住不會反覆切換模式或連續觸發互動。
- 滑鼠向右移沿用引擎右轉時「yaw 減少」的方向慣例。首輪實測後，每 pixel 由 `0x40` 降為 `0x20` binary-angle units，轉向靈敏度減半。

## 實機驗收清單

1. 冷啟動後仍是一般模式；`F2` 顯示「滑鼠視角模式：開」，再次按下顯示「關」。
2. 滑鼠左右移動方向正確、沒有自行漂移；移動速度不會過快或過慢。
3. `W/S/A/D` 分別前進、後退、左平移、右平移；方向鍵仍能前後移動及左右轉向；`Q` 仍可左移。
4. 分別對人物、門、寶箱／袋子、地上物品或屍體測試：準星進入熱區時變黃並框出單一目標；`E` 與左鍵各只觸發一次正確互動。
5. 準星對著物件時按住 `Tab`，應同時看見全部白色熱區框與目前的黃色目標框；放開後只留下目前目標。
6. 按住 `Ctrl` 後游標恢復且可以點擊右側／底部 UI；放開後游標消失、準星恢復且視角不跳動。
7. 依序開啟並離開施法、近距離地圖、世界地圖、紮營、遊戲選單、角色資訊與物品欄，確認各畫面游標正常，返回探索後自動恢復滑鼠視角。
8. 關閉模式後，A/D 左右轉、Q/E 左右平移、滑鼠點物件與 Tab 全熱區框均維持原行為。
9. 順帶確認 F5／F9、C、M、F、T、O、B 與 1／2／3 等既有熱鍵沒有迴歸。

## 首輪實測結果（2026-09-05）

- A／D 平移方向正確。
- Ctrl 暫時恢復 UI 游標正常；進出地圖、物品欄、紮營與選單後也會正常恢復視角捕捉。
- F2 關閉後的舊操作維持不變。
- 第一版滑鼠轉向過快，因此測試版已將靈敏度減半；準星 E／左鍵互動仍待實機驗收。

## 目前測試產物（2026-09-05）

- 引擎提交：`51536c6`；靈敏度修正：`0349685`。
- `dist/test_v100_zh/krondor.exe`：474384 bytes；SHA-256 `c69da9e6f26888810028062f5a40cce7f37e1879d637fdd8e5a962c3ce6ab4c8`。
- `dist/test_v100_zh/ZHSTAT.DAT`：996 glyph／21922 bytes；SHA-256 `4d2504fc5fd08cbf996ccc53dff79bb6d59a085da0f899574593a91ed91f65c0`。
- Borland C++ 3.1／Turbo Link 5.1 編譯成功；`VMCODE.OVL`、`SX.OVL` byte-identical。
- Python 測試：120 passed、1 skipped。
- 尚未建立正式 bspatch，也尚未重建 Release ZIP。
